<%= form_with(model: team_project, local: true) do |form| %>
  <% if team_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_project.errors.count, "error") %> prohibited this team_project from being saved:</h2>

      <ul>
      <% team_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.number_field :team_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.number_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon %>
  </div>

  <div class="field">
    <%= form.label :is_public %>
    <%= form.number_field :is_public %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
